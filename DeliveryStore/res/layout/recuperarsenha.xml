<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".AcessoSistema" >

    <Button
        android:id="@+id/recuperarBtRedefinir"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/redefinirEmail"
        android:layout_alignRight="@+id/redefinirEmail"
        android:layout_below="@+id/redefinirEmail"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"
        android:text="Redefinir Senha" />

    <Button
        android:id="@+id/acessoBtRecSenha"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/recuperarBtRedefinir"
        android:layout_alignRight="@+id/recuperarBtRedefinir"
        android:layout_below="@+id/recuperarBtRedefinir"
        android:layout_marginTop="16dp"
        android:text="Voltar" />

    <EditText
        android:id="@+id/redefinirEmail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:ems="10"
        android:hint="Email" >

        <requestFocus />
    </EditText>

    <TextView
        android:id="@+id/estabelecimentoCategoria"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/acessoBtRecSenha"
        android:layout_below="@+id/acessoBtRecSenha"
        android:layout_marginTop="20dp"
        android:text="Informe seu email e acesse o link que lhe redirecionará ao local para alterar sua senha." />

</RelativeLayout>
